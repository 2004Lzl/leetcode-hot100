给定两个字符串 s 和 p，找到 s 中所有 p 的 异位词 的子串，返回这些子串的起始索引。不考虑答案输出的顺序。

 

示例 1:

输入: s = "cbaebabacd", p = "abc"
输出: [0,6]
解释:
起始索引等于 0 的子串是 "cba", 它是 "abc" 的异位词。
起始索引等于 6 的子串是 "bac", 它是 "abc" 的异位词。
 示例 2:

输入: s = "abab", p = "ab"
输出: [0,1,2]
解释:
起始索引等于 0 的子串是 "ab", 它是 "ab" 的异位词。
起始索引等于 1 的子串是 "ba", 它是 "ab" 的异位词。
起始索引等于 2 的子串是 "ab", 它是 "ab" 的异位词。

answer1:
class Solution:
    def findAnagrams(self, s: str, p: str) -> List[int]:
        length=len(p)
        ans=[]
        for i in range(len(s)-length+1):
            set_s=set(s[i:i+length])
            key=True
            for j in p:
                if j not in set_s:
                    key=False
            if key is True:
                ans.append(i)
        return ans
这是我针对本题目的暴力题解,测试用例跑出来了，但是实际是存在重要问题的。
使用set只能反映出现了什么元素而不能反应元素出现了几次，只要p中有一个字符出现了两次或以上，例如p=‘aa’，而s为‘abb’，此时显然不为异位词。
因此显然不能用set
本体正确做法是滑动窗口
class Solution:
    def findAnagrams(self, s: str, p: str) -> List[int]:
        m,n=len(s),len(p)
        if m<n:
            return []
        ans=[]
        cnt_p=Counter(p)
        windows=Counter(s[:n-1])
        for i in range(n-1,m):
            windows[s[i]]+=1
            if windows==cnt_p:
                ans.append(i-n+1)
            windows[s[i-n+1]]-=1
            if windows[s[i-n+1]]==0:
                del windows[s[i-n+1]]
        return ans
首先存进去n-1个字符构成字典 随后以“字典存右侧新字符--判断与p的字典是否相同--删去左边字典”的路径 同时要注意 对于键值为零的元素 要及时删去 否则影响字典是否相同的判断
